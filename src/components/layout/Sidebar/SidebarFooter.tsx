/* SidebarFooter — intentionally empty.
   Settings and Support have moved to the Navbar. */
interface SidebarFooterProps {
  collapsed: boolean;
}

export function SidebarFooter(_props: SidebarFooterProps) {
  return null;
}
